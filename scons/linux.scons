import os

Import('context')

SConscript('unix.scons', exports = ['context'])

linuxfiles = context.cmake["ALLEGRO_SRC_LINUX_FILES"]
linuxfiles.remove("src/linux/fbcon.c")
context.add_files(linuxfiles);

