import os

Import('context')
Import('env')

linuxfiles = context.cmake["ALLEGRO_SRC_LINUX_FILES"]
# Remove .s files
linuxfiles = [x for x in linuxfiles if not x.endswith(".s")]
context.addFiles(linuxfiles);

result = SConscript('unix.scons', exports = ['context','env'])
Return('result')
