IPhone
======

This uses OpenGL ES for graphics. The touch screen is implemented as a mouse,
with multi-touch triggering different mouse buttons.

Use the xcode project in iphone-xcode, as the cmake build system does not
support iphone yet.
